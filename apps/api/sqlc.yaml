version: "2"
sql:
  - engine: "postgresql"
    # Read only the "up" migrations so sqlc sees the current schema.
    schema: "migrations/*.up.sql"
    queries: "internal/database/queries"
    gen:
      go:
        package: "db"
        out: "internal/database/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
